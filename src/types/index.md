# Types

Shared TypeScript contracts imported by the React app and Hono Worker via `@shared/types/*`.

* [index.ts](index.ts) - Re-exports upload types plus `Env`, `ApiResponse`, and standard success/error envelopes.
* [upload.ts](upload.ts) - `FileKind`, `UploadResponse`, and `UploadErrorResponse`.
